@model IEnumerable<InmobiliariaApp.Models.Propietario>
@{
    ViewData["Title"] = "Listar propietarios";
}

<h1 class="text-center p-3">Listado de propietarios</h1>


<!-- <div class="d-flex justify-content-center m-4 gap-2 ">
    <label for="search" class="d-flex align-items-end">Buscar por DNI:</label>
    <input type="text" id="search" placeholder="Ingrese el DNI del propietario"
        aria-label="Search">
    <a asp-controller="Propietario" asp-action="Detalle" class="btn btn-primary">Buscar</a>
</div> -->


<div class="d-flex justify-content-center mb-3 gap-2">
    <a class="btn btn-success" asp-action="Crear">Agregar nuevo propietario</a>
</div>

<table class="table">
    <thead class="text-black bg-light bg-gradient">
        <tr class="text-center">
            <th scope="col">DNI</th>
            <th scope="col">Nombre</th>
            <th scope="col">Apellido</th>
            <th scope="col">Telefono</th>
            <th scope="col">Email</th>
            <th scope="col">Acciones</th>
        </tr>
    </thead>
    <tbody class="text-light align-middle bg-table-p bg-gradient">
        @foreach (var propietario in Model)
        {
            <tr class="text-center">
                <td>@propietario.DNIPropietario</td>
                <td>@propietario.Nombre</td>
                <td>@propietario.Apellido</td>
                <td>@propietario.Celular</td>
                <td>@propietario.Email</td>
                <td>
                    <a class="btn btn-secondary" asp-action="Detalle" title="Detalles" asp-route-dni="@propietario.DNIPropietario">📜
                    </a>
                    <a class="btn btn-primary" asp-action="Editar" title="Editar" asp-route-dni="@propietario.DNIPropietario">✏️</a>
                    
                    <form asp-action="Eliminar" method="post" style="display:inline;">
                        <input type="hidden" name="dni" value="@propietario.DNIPropietario" />
                        <button title="Eliminar" type="submit" class="btn btn-danger"
                            onclick="return confirm('¿Seguro que deseas eliminar este propietario?');">🗑️</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>